
function greet(name, lastName) {
    console.log('Hello World ' + name + ' ' + lastName);
}

greet('Lais', 'Ziegler');
greet('Mary', 'Smith');

// Calculating a value
function square(number) {
    return number * number;
}

console.log(square(2));


// parameter is the required obj in declaration
// argument is the value supplied for that parameter

